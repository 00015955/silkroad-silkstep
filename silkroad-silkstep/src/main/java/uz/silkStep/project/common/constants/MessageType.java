package uz.silkStep.project.common.constants;

public enum MessageType {
    PLEASE_SHARE_CONTACT,
    SHARING_CONTACT,
    CONTACT_SUCCESSFULLY_SAVED,
    CONTACT_IS_NOT_YOURS,
    YOUR_OTP_CODE,
}

// MessageType → an enum that defines different types of messages that can be used in the application. Each constant represents a specific message type, such as requesting the user to share their contact information, confirming that the contact was successfully saved, or providing an OTP code. This enum can be used to standardize message handling across the application.