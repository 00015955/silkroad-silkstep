package uz.silkStep.project.enums;

public enum BookingStatus {
    PENDING, CONFIRMED, CANCELLED
}

// BookingStatus → represents the status of a booking, with possible values of PENDING (indicating that the booking is awaiting confirmation), CONFIRMED (indicating that the booking has been confirmed), and CANCELLED (indicating that the booking has been cancelled). 
//This enum is likely used to track and manage the state of bookings within the application.